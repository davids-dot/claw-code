# 进入 rust 目录
cd rust

# 查看帮助
cargo run -p rusty-claude-cli -- --help

# 交互式 REPL 模式
cargo run -p rusty-claude-cli -- --model claude-opus-4-6

# 一次性 prompt
cargo run -p rusty-claude-cli -- prompt "hello"

# 或者使用编译后的二进制
./target/debug/claw --help


export ANTHROPIC_API_KEY="sk-ant-..."
